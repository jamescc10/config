set $mod Mod4

font pango:JetBrainsMonoNL Nerd Font Mono Bold 12

# no border
for_window [class=".*"] border pixel
gaps inner 0
gaps outer 0
hide_edge_borders both

# startup
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet
exec --no-startup-id xrandr --output DP-1 --mode 1600x900 --output HDMI-2 --mode 1920x1080 --right-of DP-1 --primary
exec --no-startup-id blueman-applet
exec --no-startup-id feh --bg-fill ~/.config/wallpapers/wallpaper7.png
exec --no-startup-id picom -f --backend glx
exec --no-startup-id kwalletd6
exec --no-startup-id copyq
exec --no-startup-id ibus-daemon -rxRd

# audio
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# mod+click drag window for floating
floating_modifier $mod

# drag by title bar
tiling_drag modifier titlebar

# terminal, kill window, dmenu
bindsym $mod+Return exec kitty
bindsym $mod+Shift+q kill
bindsym $mod+d exec --no-startup-id rofi -show run

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# change focus window position
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# orientations
bindsym $mod+h split h
bindsym $mod+v split v

# fullscreen
bindsym $mod+f fullscreen toggle

# tts
bindsym Ctrl+Shift+t exec tts

# tabbed or split
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# floating window toggle
bindsym $mod+Shift+space floating toggle

# set variables for workspaces
set $ws1 "1 "
set $ws2 "2 "
set $ws3 "3 "
set $ws4 "4 "
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# set workspaces to monitors
workspace $ws1 output HDMI-2
workspace $ws2 output DP-1
workspace $ws3 output DP-1
workspace $ws4 output DP-1

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# i3 reload and restart
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# rules
for_window [class="discord"] move to workspace $ws3
for_window [class="Spotify"] move to workspace $ws4
for_window [class="Blueman-manager"] floating enable
for_window [class="feh"] floating enable

# system commands
set $mode_system System (l) lock, (e) logout, (r) reboot, (h) shutdown
mode "$mode_system" {
	# bindsym l exec --no-startup-id
	bindsym e exit, mode "default"
	bindsym r exec --no-startup-id shutdown now -r, mode "default"
	bindsym h exec --no-startup-id shutdown now, mode "default"
	bindsym Escape mode "default"
	bindsym $mod+Shift+Escape mode "default"
}
bindsym $mod+Shift+Escape mode "$mode_system"

# screenshot
bindsym Print exec --no-startup-id /home/$USER/.config/i3/screenshot-all 
bindsym Shift+Print exec --no-startup-id /home/$USER/.config/i3/screenshot-selection

bar {
	position top
        status_command i3blocks -c /home/$USER/.config/i3blocks/i3blocks.conf
	i3bar_command i3bar --transparency
	colors {
		background #2e3440CC
		separator #3b4252EE
		focused_workspace #2e3440FF #2e3440 #d8dee9
		inactive_workspace #3b4252FF #3b4252 #d8dee9
		urgent_workspace #2e3440FF #2e3400 #d8dee9
	}
	font pango: JetBrainsMonoNL Nerd Font Propo
	tray_output primary
	separator_symbol ":|:"
}
